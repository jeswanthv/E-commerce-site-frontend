import React from 'react'
import Base from "../core/Base"

const UserDashboard = () => {
    return (
        <Base title="User Dashboard" description="Check out the info!">
            <h1>Welcome to user dashboard Page</h1>
            
        </Base>
    )
}

export default UserDashboard;